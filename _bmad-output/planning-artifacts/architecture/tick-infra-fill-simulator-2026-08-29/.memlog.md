---
scope: TICK-INFRA queue-aware fill simulator (research R3, §2 of preregistration_tick_data_infrastructure.md) + its parity gate. Simulator only; the H1 strategy study is downstream.
purpose: build substrate for bmad-build to implement story by story
altitude: component (one deliverable of R3)
updated: 2026-08-29T02:28
---

- (constraint) Seal freezes all MODELLING calls (queue=back-of-queue primary / time-priority secondary; latency 250ms primary / 50ms secondary; marketable fills walk the book; passive fills need trade volume > queue-ahead; cancel/replace 1 latency hop; no own-impact; fees $0.72+commission; NOT modelled: hidden liq, variable latency, matching-engine edge cases, own-flow feedback, halts). These are read-only inputs to the architecture, not decisions to make.
- (constraint) Parity gate (Amendment 8 A8.2) is the acceptance test: Part A = replay ~28-30 real live fills from trades.db, MAE<=1 tick / p90<=2 / signed bias<=+/-0.25; Part B = >=1000 synthetic orders, 6 hard invariants at 100% (no price-improvement vs touch, limit never fills through, fill_ts>=submit+latency, queue non-neg & non-increasing, partials<=size / no phantom fill, strict causality). 15-working-day / 3-cycle kill criterion.
- (constraint) project-context.md: Python ^3.11, Poetry, Pydantic ^2 for all data models, mypy strict, black-88, | unions, list[T]. Tests in tests/unit + tests/integration, test_*.py / Test* / test_*. Package-per-capability under src/. databento 0.85.0 (needs pyproject entry).
- (direction) User chose Coaching path + spine-only deliverable (2026-08-29). Build via BMAD method is a project standing rule.
- (decision) Paradigm: discrete-event simulation with a single logical clock. One time-ordered event stream (MBO book deltas + latency-delayed order arrivals + fills), deterministic step function. Binds: every unit reasons in one monotonic sim-clock; no wall-clock, no out-of-order processing. Prevents: two units disagreeing on 'now', causality violations (Part B #6).
- (decision) Strategy<->simulator boundary: INTENT-LOG REPLAY (option A, user-approved 2026-08-29). Simulator consumes ONLY a timestamped order-intent log (submit/cancel/replace); it never calls strategy code. Parity Part A feeds the ~30 real broker orders directly. H1 is two-pass: strategy emits its intent log, then the simulator fills it. Binds: simulator is a pure function of (MBO stream, intent log, config) - no strategy code in the hot path. Prevents: simulator correctness entangled with strategy logic; non-deterministic/non-causal strategy contaminating the gate. Cost accepted: no intra-trade feedback within one simulate() call (H1 has fixed entry + fixed exit, so N/A).
- (question) OPEN: book representation (full L3 by order-id vs L2 aggregate); intermediate/cache format for the 108x H1 grid re-reads; where code lives (src/ticksim new package?); the 'three ways' - 3 runs vs 1 run 3 accountings; time/price numeric types; order state machine ownership; session-mask / degraded-day handling.
- (decision) Book: full L3, orders keyed by order_id -> (instrument_id, side, price, size, arrival_seq). One owner: simulator core folds the merged event stream into the live book; fill engine only reads it at the current sim-clock. L2/aggregate views derived, never a second stored book. Binds: single book authority. Prevents: two book impls diverging, time-priority queue model being impossible.
- (decision) Code home: new package src/ticksim/ under src/ (package-per-capability). Own tests tests/unit/test_ticksim_*.py + tests/integration. ZERO imports from src/data, src/research, src/detection, src/execution, or the async pipeline. databento is the only new external dep. Binds: isolation boundary. Prevents: entanglement with the deployed system / unused async pipeline; makes the parity gate a self-contained certification.
- (decision) Caching for the 108x H1 grid: DEFERRED to the H1 build (user-approved 2026-08-29). This build re-streams (parity gate is ~11 GB). Binding requirement so H1 isn't boxed in: the fill engine consumes an abstract ordered BookEventSource iterator - the DBN reader is one impl, a future cache is another. Revisit condition: when H1 timing on the KVM4 exceeds ~1 day per full grid pass.
- (decision) 'Three ways' (§2.3): 2 sim runs (primary config, optimistic config), 3 reports - (a) primary fills, (b) primary fills + post-hoc 1-tick adverse slip on entry+exit, (c) optimistic fills. No run shares simulator state. Report layer produces all three from the frozen OrderOutcome logs. Binds: (b) is a pure P&L transform, never a simulation variant. Prevents: one run juggling 3 P&Ls with shared mutable state; a variant being non-reproducible.
- (decision) Session mask: simulator takes explicit valid_intervals ([start,end) ns legal to simulate). Any order intent OR expected fill outside the mask => HARD-FAIL abort naming the offending order (user-approved 2026-08-29). Never silently skip a masked region. 'degraded' days flagged in the run manifest, NOT auto-excluded - analyst decides. Binds: no simulation in masked time, ever. Prevents: silent gaps corrupting a parity result.
- (decision) Numerics: int64 nanoseconds for all time; price in DBN 1e-9 fixed-point int64 until final dollar P&L; latency in ns; NO float in the hot path. MNQ tick = 250_000_000 DBN units. Binds: exact arithmetic for 'within +/-0.25 tick' comparisons. Prevents: float drift making parity tolerances non-deterministic.
- (decision) Order lifecycle: ONE component owns the state machine intent -> in_flight -> working -> {filled|cancelled|rejected|expired}, plus cancel/replace from in_flight or working. Fills and per-order diagnostics are DERIVED from its transitions, authored nowhere else. Binds: single order-state authority. Prevents: two components disagreeing on an order's state; a fill emitted that the tracker doesn't know about.
- (decision) MBO->book semantics: one tested apply_event(book, record) function is the SOLE authority on A/C/M/T/F -> book-state transitions (incl. CME/Databento trade+fill interaction). All other code treats the book as opaque (query best bid/ask, size-at-price, order-by-id, queue-ahead). Binds: single locus of microstructure knowledge. Prevents: book semantics reimplemented inconsistently across fill engine / queue model / integrity checks.
- (decision) Determinism: seeded RNG (seed in SimConfig + run manifest), no wall-clock reads, no semantic dependence on dict/set iteration order. Same (MBO stream, intent log, config) => byte-identical OrderOutcome log. Binds: full reproducibility. Prevents: a parity PASS/FAIL that can't be reproduced; Part B invariant checks that flake.
- (decision) Output: one frozen, versioned Pydantic schema OrderOutcome per order - {order_id, submit_ts, arrival_ts, terminal_state, fills[](px,size,ts), queue_rank_at_submit, queue_ahead_size, time_to_fill_ns, arrival_best_bid, arrival_best_ask, adverse_selection_flag, per-partial records}. Parity gate Parts A & B and any downstream read ONLY this schema. schema_version field; bump on any change. Binds: the simulator/consumer contract. Prevents: consumers reaching into simulator internals; silent output-shape drift.
- (decision) Fill engine = PURE DECISION FUNCTION (user-approved 2026-08-29): SimRun calls fills.decide(book, tracker, clock, config) on each relevant event; it returns fill events which SimRun applies via the tracker. Engine holds NO state; per-order queue-ahead lives on the order in the tracker; the queue model is a strategy object passed in. Binds: fill engine is stateless. Prevents: a second copy of order state fighting the OrderTracker single-owner AD.
- (decision) Module layout under src/ticksim/: config.py (SimConfig frozen + PRIMARY/OPTIMISTIC presets), book.py (L3 OrderBook + sole apply_event + queries), orders.py (OrderIntent, OrderTracker, OrderOutcome), events.py (BookEventSource protocol + DbnMboSource + ns merger), fills.py (pure engine + 2 queue models), sim.py (SimRun discrete-event loop + manifest), report.py (3-way P&L from OrderOutcome logs), parity/{part_a,part_b,gate}.py. Binds: one module per concern, names fixed. Prevents: concerns smeared across modules; builder inventing a different split.
- (decision) Layering: book.py and orders.py are leaves - import nothing from ticksim. events -> {book,orders}; fills -> {book,orders,config}; sim -> all; report -> {orders}; parity -> {sim,report,book}. No cycles. Binds: dependency direction inward to book+orders. Prevents: import cycles; book/orders acquiring upward knowledge.
- (decision) PRIMARY and OPTIMISTIC SimConfig presets are SEAL-BOUND CONSTANTS encoding preregistration §2.1 (250ms/50ms latency, back-of-queue/time-priority, $0.72 exch+reg fees). Changing a preset value is a pre-registration violation unless a new seal amendment exists. Binds: the modelling numbers live in exactly one place and are frozen. Prevents: silent drift of the decision-bearing model away from what was pre-registered.
- (decision) The 6 Part-B invariants (no price-improvement vs touch; limit never fills through; fill_ts>=submit+latency; queue non-neg & non-increasing to terminal; partials<=size / no phantom fill; strict causality) are encoded ONCE as assertion functions in parity/, consumed by BOTH the >=1000-order Part B gate run AND the tests/unit suite on tiny hand-built books. Binds: invariants defined once, checked at two scales. Prevents: the gate and the unit tests checking subtly different things.
- (decision) Part A intent log is RECONSTRUCTED from live order records (data/mim_nb/orders.csv and yank's equivalent where they carry real order type + limit price; trades.db row otherwise), preserving the real order type (mim-nb/yank use bracket orders: limit entry, TP-limit / SL-stop exit - NOT plain marketable). The live broker fill price is the comparison TARGET, never a simulator input. Binds: Part A replays real orders, not real outcomes. Prevents: feeding the answer back in; a false parity PASS from marketable-order assumptions.
- (assumption) [ASSUMPTION] yank live-combine orders have a machine-readable order record with type+price somewhere analogous to data/mim_nb/orders.csv; if only trades.db exists for yank, its 8 fills fall back to reconstructed bracket orders using tp_price/sl_price from the metadata JSON. To verify at Part A story time.
- (event) Reviewer gate ran: lint PASS (0); rubric PASS-WITH-FIXES (4 blocking); adversarial NEEDS-WORK (15 findings); version-verify PASS (1 low). Core structure judged sound; findings are seam underspecification. Applying all as new/tightened ADs.
- (decision) GATE FIX - Canonical event order (AD-20): clock = ts_event. Total order (ts_event, class_rank, sequence) with class_rank book_delta(0) < order_arrival(1) < fill_apply(2). Per-tick: fold ALL book deltas at T via apply_event -> inject arrivals at T -> fills.decide once -> sim applies FillEvents. Passive 'volume after arrival' = trades at ts_event strictly > our arrival ts_event (same-T trades sort before arrival, not counted). DbnMboSource yields file order; merge_streams is a STABLE k-way merge. Closes adversarial F1.
- (decision) GATE FIX - FillEvent (AD-19): frozen model in orders.py next to Fill: {order_id, px_dbn:int, size:int, ts_ns:int}. STRICTLY this-tick incremental. Carries NO queue-rank / adverse-selection. sim applies each via one named tracker method (sole path to a partial/terminal fill transition per AD-8). Closes adversarial F2.
- (decision) GATE FIX - Book-event -> order-state seam (AD-21): sim is sole driver. After each apply_event, sim calls queue_model.observe_book_event(tracker, record). Enumerated: T at/through our price decrements queue_ahead; C and size-reducing M of resting orders AHEAD of us (by add_ts_ns) decrement it; nothing else. fills.decide stays pure of the resulting tracker state. Ordering: apply_event -> observe_book_event -> arrivals -> fills.decide. Closes adversarial F3.
- (decision) GATE FIX - Book is venue-faithful ONLY (AD-3 tightened + AD-22): our orders are NEVER inserted into the L3 book; they live only in OrderTracker. Per-resting-order tuple gains add_ts_ns (the A record's ts_event). One shared interface: queue_ahead_size(book, side, price_dbn, our_arrival_ts_ns)->int, implemented identically by both queue models. queue_rank_at_submit / queue_ahead_size_at_submit computed EXACTLY ONCE by sim at the arrival tick, written to the tracker order; report/parity read only from OrderOutcome. Single back-of-queue fill formula written in the AD. Closes adversarial F4, F10, F12.
- (decision) GATE FIX - per-order vs per-trade (AD-12 tightened + AD-23): OrderOutcome adds trade_id (opaque, from the intent-log producer) and leg in {entry,exit}. kind FROZEN as {marketable, marketable_limit, passive_limit}. NEW AD-23: OrderIntent is a frozen versioned Pydantic schema (parallel to AD-12) - {schema_version, trade_id, leg, order_id, kind, side, size, limit_px_dbn|None, submit_ts_ns, action in {submit,cancel,replace}, replaces_order_id|None}. Replace convention: one record with action=replace reusing order_id (never cancel+new). Closes adversarial F5, rubric F2/F6.
- (decision) GATE FIX - monetary values report-layer only (AD-24, + AD-10/AD-12 tightened): NO monetary field ever in OrderOutcome. report.py reads fees/commission/point-multiplier from the manifest's SimConfig dump (a run artifact, explicitly permitted - AD-12 amended to 'OrderOutcome for fills + manifest for config/fees'). config.py holds DOLLARS_PER_INDEX_POINT=2 as a named seal-cited constant, report.py its only consumer. PRIMARY/OPTIMISTIC bake seal-default commission $0.58; an override builds a derived config recorded in the manifest, output is §6-secondary only (resolves the AD-15 'configurable vs frozen' contradiction). Closes adversarial F6.
- (decision) GATE FIX - AD-13 SPLIT into three: (a) intent-log validation - submit/cancel/replace ts outside the mask => IntentLogError (bad analyst input); (b) AD-8 gains a trigger: a working order is force-transitioned working->expired at the end of its containing valid_interval, yielding a normal 'expired' OrderOutcome; (c) a fill outside the mask is then a true InvariantViolation (must be impossible). Intervals are [start_ns, end_ns) half-open. Closes adversarial F7.
- (decision) GATE FIX - AD-25 OCO/bracket groups: a first-class OrderTracker concept. A bracket = a linked group (entry + tp + sl). When any leg fills, the tracker deterministically auto-cancels the siblings. This is bookkeeping the broker also does, NOT strategy feedback - it does not violate AD-2 (no new intent is generated; only cancels of already-submitted siblings). part_a reconstructs the real bracket; H1 uses brackets natively. Closes rubric F2 fully + adversarial F5 bracket concern.
- (decision) GATE FIX - AD-26 parity gate output: exact amendment-stub template (sample N; 3 Part-A figures vs the 3 tolerances; per-trader breakdown; Part-B per-invariant violation counts; simulator SHA via an explicitly-permitted subprocess 24a122e6b8cc474ec79c29c334d4f72926a48972; verdict; cycle number). Part B runs ALL >=1000 synthetic orders, collects EVERY violation, FAILs if the set is non-empty (not fail-fast). Cycle-count / 15-day kill-criterion bookkeeping is the analyst's, out of code, recorded in the amendment. Closes adversarial F8.
- (decision) GATE FIX - AD-27 parity thresholds are seal-bound constants (config.py, seal-cited, change-needs-amendment, like AD-15): PARITY_MAE_MAX_TICKS=1.0, PARITY_P90_MAX_TICKS=2.0, PARITY_SIGNED_BIAS_MAX_TICKS=0.25, PART_A_MIN_N=28, MNQ_TICK_DBN=250_000_000. Gate verdict = Part A PASS AND Part B PASS. Closes rubric F3.
- (decision) GATE FIX - AD-28 adverse_selection: computed by sim via a bounded 1s-deferred check queue; the tracker order stays mutable for THIS ONE FIELD ONLY until fill_ts + 1s (or run end); NOT a second replay (AD-14's 'exactly two SimRuns' holds). Closes adversarial F9.
- (decision) GATE FIX - smaller: AD-4 allowed new deps = databento + sortedcontainers (tiny pure-Python; needed for the O(log n) book per Amendment 9's measured O(n^2) bottleneck). AD-9 tightened: apply_event MUST tolerate C/M for an unseen order_id (Mode-C warm-up, Amendment 9 A9.2: 0.3% of C/M reference pre-window orders) - treat as no-op + count. AD-2 tightened: sim VALIDATES the intent log is causally orderable (monotonic submit_ts, no replace-before-submit, no cancel of unknown id); deeper non-causality is the producer's responsibility, explicitly out of enforcement scope. AD-18 tightened: BookEventSource yields L3 MBO-equivalent per-order events (A/C/M/T/F with order_id), not merely 'book events'. AD-11 tightened: byte-identity scoped to the OrderOutcome log, NOT the manifest. AD-10 tightened: add a test asserting every numeric in the OrderOutcome JSONL is integer.
- (decision) GATE FIX - structural: run-id = sha256(source-bytes-manifest || intent-log-sha || config-hash) (deterministic, AD-11). The PRIMARY/OPTIMISTIC pair lives under one study-id/ with primary/ and optimistic/ subdirs; each manifest names its sibling. Part B runs PER-WINDOW over the 28 disjoint Tranche-1 windows, pools results; synthetic order ts drawn only within each window's interval, >= warm-up offset from window start. databento pyproject.toml entry MOVED from Deferred into the first story's acceptance (Amendment 9 A9.5 makes it a required pre-build step). MAX_TRANSIENT_CROSS_NS named in config.py (default 50_000_000 = 50ms, conservative; Amendment 9 A9.3 refined the seal's 100.000% check).
- (decision) GATE FIX - AD-17 tightened: (a) unfilled sim exit in part_a where the real exit filled => a defined-magnitude parity miss (sim fill price = the touch it could have crossed at exit_ts + the model's slippage), recorded and counted, never silently excluded; (b) the replace convention from AD-23 applies to part_a's reconstruction identically. Deferred 'multi-instrument no rework' claim SOFTENED to 'book is per-id; OrderTracker/report/valid_intervals/merged-stream would need per-id keys'.
- (event) All reviewer findings triaged and folded into ADs 19-28 + tightenings of AD-2/3/4/9/10/11/12/13/17/18. Re-distilling the spine.
- (event) spine finalized — 28 ADs, lint clean, all 19 reviewer findings folded in. status: final.
