# Imported local evidence — not independent research

Accessed 2026-09-13. Sources are the exact local paths and SHA-256 identities in `local-data-inventory.json`. The source files were inspected this run; this import records their scope and does not promote historical strategy performance claims.

1. Current `mnq_1min_2025.csv` SHA-256 is `3f20ec70885cdee6b48e6c5c7ed3254dd4cc8ce7bd8533696c5e461c75fb7822`, identical to the input identified by `docs/reports/yank-provenance-closure/README.md`. The retained `historical-writer.py.txt` accumulates complete source records until a dollar threshold is reached, then emits the last constituent timestamp and total volume. The earlier audit reports exact byte reproduction and mixed-contract constituents; those historical lineage findings were imported, not rerun. This run independently verified current file identity and inspected the writer code. Therefore no assumption of fixed one-minute construction is admissible.
2. Timestamp-only inventory measured four candidate CSVs. Counts are observations, not independent samples; steps longer than a minute include legitimate market closures. No historical exchange calendar or source timestamp semantics were established by that inventory. CSVs omit explicit contract identity. No strategy statistics were computed.
3. `data/tick/mnq_mbo_parity/windows_manifest.json` explicitly says selection was centered on parity fills plus/minus 90 minutes, merged into 28 windows and 5,154 minutes. The selection is not a representative full-Globex session sample. Additional top-up files are present, but filename presence does not establish complete session coverage or independent selection. No tick prices were decoded in this run.
4. `scripts/update_eod_data.py` contains an updater targeting `MNQM26` and appending to the 2026 CSV while dropping contract identity. This is evidence of a possible writer path, not proof of the entire file's origin or its execution history.
5. `study_volume_profile_poc.py` declares use of 2025 through February 2026; the VPOC-1 pre-registration identifies related development/holdout periods. These establish prior experiment definitions and data-reuse concerns, not their profitability. No currently inspected period was certified as untouched.

No data inside `data/sealed_holdout/`, credentials, live ledger or raw mixed-year historical store was accessed. Local sources can establish this workspace's state; they cannot establish the strategy's public scientific validity.

## Additional native-data discovery

Metadata-only search also found an existing MNQM5 MBO/status/definition pilot requested from 2025-05-19 00:00 UTC through 2025-05-31 00:00 UTC, plus short June-22 2026 windows. Exact source metadata and hashes are in `additional-native-metadata.json`. The May pilot is a concrete candidate for future profile-reconstruction/measurement work; it is not part of the 28-window parity manifest. Its UTC-midnight first boundary is after the Globex opening, and full eligible interior sessions require calendar/event completeness checks. This run did not decode native market records, certify its representativeness or use it for power/strategy returns. The report must not imply no native multi-day history exists.
