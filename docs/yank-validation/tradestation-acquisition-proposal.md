# TradeStation development-data acquisition proposal

Decision: HOLD_VALIDATION. Proposal only: no authentication, requests, purchase or collection has occurred.

Acquire unadjusted one-minute bars for explicit MNQH25, MNQM25, MNQU25 and MNQZ25 contracts covering their 2025 development segments. Resolve provider spellings and exact expiration/session metadata first; never substitute @MNQ continuous bars. Pin the segment/roll calendar before a request. Each segment needs same-contract warm-up: at least 120 completed H1 volatility observations plus 20 H1 ATR prehistory, 1,950 M1 LR rows and 20 preceding observed daily ranges. Request at least 30 calendar days before each segment, extend only to satisfy documented coverage requirements, and report any unavailable history. January's candidate same-contract MNQH25 warm-up begins 2024-12-01; this is a future acquisition proposal, not authorization to read additional data now.

For each request retain a credential-free request ID and request hash; symbol, interval=1, unit=Minute; exact first/last UTC boundaries; request start, response receipt and server timestamps if supplied; pagination/chunk identity; HTTP result; raw response bytes/hash; original row order; per-row status/completion flags; server/provider/version metadata; price precision and adjustment policy. Keep auth headers, cookies, tokens and account identifiers out of artifacts. Preserve raw responses separately from normalized bars. Overlap chunk boundaries deliberately to quantify duplicates and revisions rather than silently deduplicating them.

Verify start/end label semantics and unfinished-bar behavior using provider documentation and separately approved prospective observation. A historical finalized endpoint alone cannot answer receipt-time semantics. Verify per-contract exchange-session/DST coverage, holidays, scheduled closures, missing minutes, duplicate/revised/out-of-order rows, late completion and first/last chunk truncation. Do not invent bars. Run full H1/M15/LR/ADR readiness checks before marking any decision assessable. Native-vs-TradeStation disagreements are source suitability findings, not requests to retune strategy parameters.

Cost worksheet (not a current price quote): a full year has at most 525,600 calendar minute slots; actual tradable bars are fewer. Budget four explicit contract segments plus their warm-up and overlap. If the verified endpoint maximum is B bars/request, estimated calls are sum(ceil(segment_and_warmup_bars / B)) plus overlap/retry allowance. Record B, rate limit, entitlement and any per-request/per-row charge from the provider before execution. Estimated data charge is zero only if existing entitlement explicitly includes this history without incremental fees; otherwise cost is UNKNOWN until quoted. Dollar authorization for this proposal is $0. No order, purchase or paid request is authorized by these files.

Deliver immutable raw response archives, a pin manifest, contract/calendar mapping, coverage ledger, admission result and two deterministic offline diagnostic runs. Retain HOLD_VALIDATION until engineering fidelity, TradeStation semantics, warm-up, account/risk and observed ProjectX execution evidence are independently established.
